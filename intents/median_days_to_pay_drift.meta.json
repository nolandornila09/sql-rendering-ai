{
  "template_id": "median_days_to_pay_drift",
  "required_filters": ["tenant_id", "as_of_date"],
  "defaults": {
    "weeks": 8,
    "min_delta_days": 5,
    "sort_direction" : "DESC",
    "limit" : 100
  },
   "param_formats": {
    "tenant_id": "string",
    "as_of_date": "date (YYYY-MM-DD)",
    "weeks": "integer",
    "min_delta_days": "integer",
    "sort_direction" : "string",
    "limit" : "integer"
  },
  "projected_columns": [
    "customer_id",
    "delta_days"
  ],
  "partitions": ["tenant_id","yyyy_mm"]
  }
